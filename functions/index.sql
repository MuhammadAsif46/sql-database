-- Aggregate functions: (most popular)

--Get Count name:
SELECT COUNT(names) FROM student;

--Get maximum marks:
SELECT MAX(marks) FROM student;

--Get minimum marks:
SELECT MIN(marks) FROM student;

--Get avergae marks:
SELECT AVG(marks) FROM student;

-- Get total marks:
SELECT SUM(marks) FROM student;



-- Group by clause: